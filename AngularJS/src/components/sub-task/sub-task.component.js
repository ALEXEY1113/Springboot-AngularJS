(function() {
  'use strict';

  // Usage:
  // 
  // Creates:
  // 

  angular
    .module('course')
    .component('subTask', {
      templateUrl: 'components/sub-task/sub-task.html',
      controller: 'subTaskController',
      bindings: {
      }
    });
})();